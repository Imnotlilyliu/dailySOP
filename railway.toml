[build]
buildCommand = "npm install --legacy-peer-deps --no-audit --no-fund"

[deploy]
startCommand = "prisma contract emit && prisma db update --db $DATABASE_URL && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 30

[[deploy.restart]]
disable = false
maxRetries = 3
